# Store graphics still needed

The existing `phoneScreenshots/` contains three 1080×2340 screenshots (wheel
depths and Sankey). Review their content before submission and add a journal
screenshot with deliberately created demo entries if useful.

Still needed: the final 512×512 PNG store icon as `icon.png` and a 1024×500
feature graphic as `featureGraphic.png`. Review current Play Console image
requirements before uploading. No placeholder graphics are supplied.

F-Droid reads this directory. The release workflow uploads Play binaries and
release notes; initial listing text and graphics are entered in Play Console.
